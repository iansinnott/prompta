/**
 * This is the name of the db the app will connect to locally for now. Changing
 * it effectively resets the db, without data loss since you can change it back.
 * This is good for testing as if on a new system.
 */
export const DB_NAME = "chat_db-v8.1";
